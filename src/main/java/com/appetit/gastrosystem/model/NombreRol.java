package com.appetit.gastrosystem.model;

/**
 * Roles soportados por la plataforma GastroSystem.
 * Debe coincidir exactamente con los valores insertados
 * en la tabla `roles` (columna nombre).
 */
public enum NombreRol {
    ADMINISTRADOR,
    MESERO,
    COCINA,
    DOMICILIARIO,
    CLIENTE
}
